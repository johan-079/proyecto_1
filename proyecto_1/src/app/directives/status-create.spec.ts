import { StatusCreate } from './status-create';

describe('StatusCreate', () => {
  it('should create an instance', () => {
    const directive = new StatusCreate();
    expect(directive).toBeTruthy();
  });
});
